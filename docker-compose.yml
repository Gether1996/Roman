name: masaze_vlcince

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "127.0.0.1:9000:8000"
    env_file:
      - .env
    environment:
      TZ: Europe/Bratislava
    restart: unless-stopped  