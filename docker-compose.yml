name: masaze_vlcince

services:
  web:
    build: .
    ports:
      - "127.0.0.1:9000:8000"
    env_file:
      - .env
    volumes:
      - app_data:/data
    restart: unless-stopped

volumes:
  app_data:
